package com.bierbock.BackendFolder;

import com.bierbock.MainActivity;

import org.json.JSONObject;

/*
public class BestSearchResults extends BackendRequest{

    public BestSearchResults(String searchString, MainActivity activity) {
        super("GET", "https://www.beerbock.de/BierBock/bestSearchResults", result -> {
            JSONObject obj;

            obj = new JSONObject(result);

            if ("Successful".equals(obj.getString("statusMessage"))) {
                // TODO: Implement update UI
            } else {
                // TODO: Implement
            }
        });

        String body = String.format("{\"searchString\": \"%s\"}", searchString);

        execute(body);
    }
} */
