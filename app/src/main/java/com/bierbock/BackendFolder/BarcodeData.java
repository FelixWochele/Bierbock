package com.bierbock.BackendFolder;

import com.bierbock.MainActivity;

import org.json.JSONObject;

/*
public class BarcodeData extends BackendRequest{

    public BarcodeData(String barcode, MainActivity activity) {
        super("GET", "https://www.beerbock.de/BierBock/barcodeData", result -> {
            JSONObject obj;

            obj = new JSONObject(result);

            if ("Successful".equals(obj.getString("statusMessage"))) {
                // TODO: Implement update UI
            } else {
                // TODO: Implement
            }
        });

        String body = String.format("{\"barcode\": \"%s\"}", barcode);

        execute(body);
    }
} */
